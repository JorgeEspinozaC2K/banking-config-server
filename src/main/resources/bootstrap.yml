server:
  port: 8090
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JorgeEspinozaC2K/banking-config-data.git
          search-paths: /
          default-label: "master"
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}     
  security:
    user:
      name: ${security_name}
      password: ${security_password}
